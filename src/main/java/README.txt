-- !! comando para mudar o ambiente por linha de código !! --

java -jar .\target\algafood-api-0.0.1-SNAPSHOT.jar -Dspring.profiles.active=pro

**ps**
as configurações de coneção tambem podem ser mudadas no application.properties.

